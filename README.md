📌 Додавання файлів до коміту
git add .
всі файли

git add filename
Додає конкретний файл.

📌 Створення коміту
git commit -m "Commit message"


📌 Перегляд історії комітів
git log


Повна історія комітів.
git log --oneline


📌 Гілки (Branches)---

git branch
Показує всі гілки.


git branch branch-name
Створює нову гілку.


git checkout branch-name
Перемикається на іншу гілку.


git checkout -b branch-name
Створює гілку та одразу переходить у неї.


📌 Злиття гілок------
git merge branch-name
Зливає вказану гілку в поточну.


git remote add origin https://github.com/username/repository.git
Підключає віддалений репозиторій.


git remote -v
Показує підключені віддалені репозиторії.

📌 Відправка змін на сервер-----
git push
Відправляє коміти у віддалений репозиторій.

git push -u origin branch-name
Відправляє гілку та робить її основною для push.

📌 Отримання змін з сервера-------
git pull
Завантажує та одразу зливає зміни з віддаленого репозиторію.

git fetch
Завантажує зміни без злиття.

📌 Порівняння змін-----
git diff
Показує різницю між поточними змінами та останнім комітом.

📌 Скасування змін--------
git checkout -- filename
Скасовує локальні зміни у файлі.

git reset --soft HEAD~1
Відміняє останній коміт, залишаючи зміни.

git reset --hard HEAD~1
⚠️ Повністю видаляє останній коміт та всі зміни.

📌 Файл .gitignore-------

Файл .gitignore дозволяє виключити файли з відстеження Git.
Приклад:

node_modules/
.env
dist/
.idea/

📌 Типовий робочий процес
git checkout -b feature-name
git add .
git commit -m "Add new feature"
git push -u origin feature-name